import React from 'react';

// Calls animation for loading page
const LoadingPage = () => (
    <div className="loader">
        <img className="loader__image" src="/img/monkeywalk3.gif" />
    </div>
);

export default LoadingPage;
